apiVersion: apps/v1
kind: Deployment
metadata:
  name: eco-bot
  namespace: default
  labels:
    app: eco-bot
spec:
  replicas: 1
  selector:
    matchLabels:
      app: eco-bot
  template:
    metadata:
      labels:
        app: eco-bot
    spec:
      containers:
      - name: eco-bot
        image: romanmartyny/bot:dcb93db39294aadbf5de36ca21dfb62801575e43
        imagePullPolicy: Always
        env:
        - name: BOT-API-KEY
          valueFrom:
            secretKeyRef:
              name: eco-bot-secret
              key: token
        - name: BACKEND_URL
          value: "http://eco-backend:8000"
        resources:
          requests:
            memory: "64Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "500m"
